rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents
    // but only allow writes if the user is authenticated.
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
